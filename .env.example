# OpenAI (LangChain 감성분석 + 임베딩) — 필수
OPENAI_API_KEY=sk-...

# 모델 (선택)
OPENAI_CHAT_MODEL=gpt-4o-mini
OPENAI_EMBED_MODEL=text-embedding-3-small

# Chroma 영속 디렉토리
CHROMA_DIR=./chroma_db

# 시장 (us | kr) — 장마감 윈도우 계산용
MARKET=us
